import { styled, Typography } from "@material-ui/core";

const AccordionHeader = styled(Typography)({
    color: '#eee',
    fontWeight: 600
})
const AccordionText = styled(Typography)({
    color: '#eee',
})

export { AccordionHeader, AccordionText }